{% extends 'base.html.twig' %}

{% block title %}Fichier{% endblock %}

{% block body %}
    <h1>Fichier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fichier.Nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fichier.Description }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ fichier.Lien }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fichier.createdAt ? fichier.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ fichier.updatedAt ? fichier.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fichier_index') }}">back to list</a>

    <a href="{{ path('fichier_edit', {'id': fichier.id}) }}">edit</a>

    {{ include('fichier/_delete_form.html.twig') }}
{% endblock %}
